
  # KSL Prototype

  This is a code bundle for KSL Prototype. The original project is available at https://www.figma.com/design/PmagoV6kyppJiYFqPiXgo0/KSL-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  